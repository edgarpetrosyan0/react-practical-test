import styles from './styles.module.scss';
        
interface UserCardProps { // TODO extends User entity type which must be declared global

}

export const UserCard: React.FC<UserCardProps> = () => {
    return (
        <div>
            there show user simple info
        </div>
    );
};